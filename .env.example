# Copy this file to .env.local (gitignored — never commit real values there,
# though the anon key is safe to expose in client code by design, protected
# by the RLS policies in supabase/schema.sql) and fill in your Supabase
# project's values from Settings -> API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
